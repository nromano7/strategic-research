// DECKS
{
  "query": {
     "bool": {
        "must": [],
        "should": [
          
          {"match": {"title": "deck"}},
          {"match": {"title.bigram": "bridge deck"}},
          {"match": {"title.bigram": "concrete deck"}},
          {"match": {"title.bigram": "reinforced deck"}},
          {"match": {"title.trigram": "reinforced bridge deck"}},
          {"match": {"title.trigram": "reinforced concrete deck"}},
          {"match": {"title.quadragram": "reinforced concrete bridge deck"}},
          
          {"match": {"abstract": "deck"}},
          {"match": {"abstract.bigram": "bridge deck"}},
          {"match": {"abstract.bigram": "concrete deck"}},
          {"match": {"abstract.bigram": "reinforced deck"}},
          {"match": {"abstract.trigram": "reinforced bridge deck"}},
          {"match": {"abstract.trigram": "reinforced concrete deck"}},
          {"match": {"abstract.quadragram": "reinforced concrete bridge deck"}}
        ],
        "must_not": []
     }
  }
}

// 